# Controls whether the Umami tracker script is injected.
# Unset = production builds only (import.meta.env.PROD).
# Set to "true" to load on a local dev server; "false" to force-disable everywhere.
#
# PUBLIC_UMAMI_ENABLED=true
